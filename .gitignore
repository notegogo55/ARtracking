# Environments
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

# Data & outputs at repo root only (cached arrays, downloads, experiment artifacts);
# patterns are anchored so the solarflare/data package stays tracked.
/data/*
!/data/.gitkeep
!/data/weights/
/outputs/

# Notebooks checkpoints
notebooks/.ipynb_checkpoints/

# Claude Code personal settings
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
